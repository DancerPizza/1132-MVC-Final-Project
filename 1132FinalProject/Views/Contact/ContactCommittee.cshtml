@model IEnumerable<_1132FinalProject.Models.ContactCommitteesModel>
@{
    ViewData["Title"] = "聯絡幹部";
}

<div class="d-flex flex-wrap justify-content-center gap-4">
    @foreach (var card in Model)
    {
        @Html.Partial("_CommitteesCard", card)
    }
</div>